const parts = ["overlay", "dialogContainer", "dialog", "header", "closeButton", "body", "footer"];

const modal = {
  parts,
  baseStyle: {
    // dialog: {},
    body: {
      padding: 0
    }
  }
};

export default modal;
